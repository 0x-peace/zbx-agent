---
zabbix_version: "3.4"
zbx_srv: "zabbix.egs.kz"
zabbix_agent_tls: True
zabbix_repo: "http://repo.zabbix.com/zabbix/{{ zabbix_version }}/{{ ansible_distribution|lower }}"

zabbix_register: True
zabbix_server_url: "https://zabbix.egs.kz/zabbix"
zabbix_server_user: "ikondratyev"
zabbix_server_pass: "m82zsvf51"
zabbix:
  host_groups:
    - egs
  host_templates:
    - OS Linux.Base
    - OS Linux.FS
    - OS Linux.Network
